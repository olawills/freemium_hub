import 'package:flutter/material.dart';

class WallpaperSettings extends StatelessWidget {
  const WallpaperSettings({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
